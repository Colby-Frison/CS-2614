I want you to rewrite the design document again, but with some changes.

First of all make sure you write the document as if you were a college junior in computer science, meaning your writing is good, but not perfect, and your knowledge of the subject is mostly good, but not 100% there. Most importantly write the document like a human

I want you to fully explain how everything will happen without actually using any assembly code. You can use a sort of psuedo code with raeafrences to the fuinction that it will probably use, but do not actually write any assmebly code.

Make sure to answer these questions:
- How to achieve looping in assembly language? (Hints: assembly language has no built-in
while loop and for loop)
- Specifically, what are the loop conditions (initial values, end values, increments in each
step, stopping conditions)?

Also make sure you implement the following criteria
1. Mention the assembly instructions in the problem solving approach.
2. How to take in and convert input characters into decimal numbers (ASCII to decimal)
3. Loop conditions for calculating the sum
4. How to convert the result into cotal number and display it
5. Keep the document well organized and use good grammar

The main point of this is being able to break the big problem into subproblems that are easier to tackle in the assembler.

Also make create the program in python, as a reminder here is the **problem statement**:

**Given a 2-digits odd decimal number n as user input, write an assembly language program to calculate
the sum of odd positive numbers up to that odd decimal number, and then display the sum in octal
number.**
